# mini-spatail-data-plateform

This is project in main branch use MongoDB set up is Dockerfile, Docker Compose, config database, connect database and migrate, but not complete  insert data pass but not yet understand make index and manage index. So let's hold this project. Change to use relation database is postgres to make sure to submit the work on time. Another branch is make same project but use postgres database
